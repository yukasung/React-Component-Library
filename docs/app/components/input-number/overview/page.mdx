import { Callout } from 'nextra/components'
import { DefaultDemo, DisabledDemo, FormatDemo, HandleWheelDemo, IsRequiredDemo, MinMaxDemo, PlaceholderDemo, ReadOnlyDemo, RepeatButtonsDemo, StepDemo, ValueDemo } from './demo'
import { PropertyIndex, PropertySignature } from './property-index'

export const metadata = {
  title: 'InputNumber',
  description: 'Input สำหรับกรอกตัวเลข พร้อมปุ่มเพิ่ม/ลด, จำกัดค่า min/max, และการจัดรูปแบบทศนิยม',
}

# InputNumber

InputNumber คือ input ที่ให้ผู้ใช้กรอกและแก้ไขได้เฉพาะตัวเลขเท่านั้น ค่าที่กรอกสามารถจัดรูปแบบหรือจำกัดให้อยู่ในช่วงที่กำหนดได้

<DefaultDemo />

## Import

```tsx
import { InputNumber } from '@your-scope/components'
```

## Properties

<PropertyIndex />

### value

<PropertySignature name="value" type="number | null" />

กำหนดค่าปัจจุบันของ control

<ValueDemo />

```tsx
const [value, setValue] = useState<number | null>(10)
<InputNumber value={value} onChange={setValue} />
```

### placeholder

<PropertySignature name="placeholder" type="string" />

กำหนดข้อความที่แสดงเป็น hint เมื่อ control ว่างเปล่า

<Callout type="info">
  **Note**

  ต้องตั้ง isRequired เป็น false ด้วย ไม่เช่นนั้นฟิลด์จะไม่มีทางว่างเปล่า (บังคับแสดงเป็น 0 เสมอ)
</Callout>

<PlaceholderDemo />

```tsx
<InputNumber isRequired={false} defaultValue={null} placeholder="เช่น 100" />
```

### isRequired

<PropertySignature name="isRequired" type="boolean" />

กำหนดค่าที่ระบุว่าค่าของ control ต้องเป็นตัวเลขเสมอ หรือสามารถตั้งเป็น null ได้ (โดยการลบเนื้อหาในช่องออกทั้งหมด)

ค่าเริ่มต้นของ property นี้คือ true

<IsRequiredDemo />

```tsx
<InputNumber isRequired={false} defaultValue={null} />
```

### min

<PropertySignature name="min" type="number" />

กำหนดค่าต่ำสุดที่ยอมรับได้ การพิมพ์ที่จะทำให้ค่าต่ำกว่านี้ถูกบล็อกทันที

<Callout type="info">
  **Note**

  ไม่ระบุหมายถึงไม่จำกัดค่าต่ำสุด
</Callout>

### max

<PropertySignature name="max" type="number" />

กำหนดค่าสูงสุดที่ยอมรับได้ การพิมพ์ที่จะทำให้ค่าเกินนี้ถูกบล็อกทันที

<Callout type="info">
  **Note**

  ไม่ระบุหมายถึงไม่จำกัดค่าสูงสุด
</Callout>

<MinMaxDemo />

```tsx
<InputNumber min={0} max={100} defaultValue={50} />
```

### step

<PropertySignature name="step" type="number | null" />

กำหนดจำนวนที่จะเพิ่มหรือลดค่า value เมื่อผู้ใช้กดปุ่ม spinner

ค่าเริ่มต้นของ property นี้คือ null ซึ่งจะซ่อนปุ่ม spinner จาก control

<Callout type="info">
  **Note**

  step เป็นตัวกำหนดว่าจะแสดงปุ่ม spinner หรือไม่ ไม่มี prop แยกสำหรับเรื่องนี้

  ถ้าไม่ระบุ step ปุ่ม spinner, ปุ่มลูกศรบนคีย์บอร์ด และ handleWheel จะใช้งานไม่ได้ทั้งหมด เพราะไม่มีจำนวนให้ขยับค่า

  ถ้า step มีทศนิยมและไม่ได้ระบุ precision เอง ระบบจะคำนวณจำนวนทศนิยมที่แสดงให้อัตโนมัติ
</Callout>

<StepDemo />

```tsx
<InputNumber defaultValue={1} />
<InputNumber step={0.1} defaultValue={1} />
```

### format

<PropertySignature name="format" type="string" />

รูปแบบการแสดงผลตัวเลข เขียนแบบ .NET standard numeric format string — ตัวอักษร 1 ตัวบอกประเภท ตามด้วยจำนวนทศนิยมที่เป็นตัวเลือก (เช่น "n2", "C", "p0") เมื่อระบุจะแทนที่ precision เดิมทั้งหมด

รองรับครบทั้ง 9 ตัว:

| ตัวอักษร | ความหมาย | ตัวอย่าง (ค่า 1234.5) |
| --- | --- | --- |
| N | ตัวเลขมี comma คั่นหลักพัน | 1,234.50 |
| C | สกุลเงิน (นำหน้าด้วย $) | $1,234.50 |
| P | เปอร์เซ็นต์ (คูณค่าด้วย 100 แล้วต่อท้ายด้วย %) | 123,450.00% |
| F | ทศนิยมธรรมดา ไม่มี comma | 1234.50 |
| D | จำนวนเต็ม (ตัดทศนิยมทิ้ง) เติม 0 ด้านหน้าได้ | 1234 |
| E | สัญกรณ์วิทยาศาสตร์ | 1.234500E+003 |
| G | เลือกรูปแบบกะทัดรัดที่สุดระหว่างทศนิยมกับวิทยาศาสตร์ให้เอง | 1234.5 |
| X | เลขฐาน 16 (ตัดทศนิยมทิ้งก่อน) | 4D2 |
| R | ค่าที่แปลงกลับเป็นตัวเลขเดิมได้เป๊ะที่สุด | 1234.5 |

ตัวเลขที่ต่อจากตัวอักษร (เช่น "n2") กำหนดจำนวนทศนิยมที่แสดง ยกเว้น D และ X ที่หมายถึงจำนวนหลักขั้นต่ำแทน ไม่ระบุจะใช้ค่า default ของตัวอักษรนั้น

<Callout type="info">
  **Note**

  ค่าติดลบแสดงด้วยเครื่องหมายลบนำหน้าเสมอ การจัดรูปแบบเกิดขึ้นระหว่างพิมพ์จริง ไม่ใช่แค่ตอน commit
</Callout>

<FormatDemo />

```tsx
<InputNumber format="n0" defaultValue={0} />
<InputNumber format="n2" defaultValue={0} />
<InputNumber format="c0" defaultValue={0} />
<InputNumber format="c2" defaultValue={0} />
<InputNumber format="p0" defaultValue={0} />
<InputNumber format="p2" defaultValue={0} />
```

### repeatButtons

<PropertySignature name="repeatButtons" type="boolean" />

กำหนดว่ากดปุ่ม spin ค้างไว้แล้วให้เพิ่ม/ลดค่าซ้ำต่อเนื่องหรือไม่ ค่าเริ่มต้น (default): true

<Callout type="info">
  **Note**

  กดครั้งเดียวโดยไม่ค้างยังคงขยับ 1 step ตามปกติเสมอ ไม่ว่าตั้งค่านี้เป็นอะไร
</Callout>

<RepeatButtonsDemo />

```tsx
<InputNumber repeatButtons={false} defaultValue={10} />
```

### handleWheel

<PropertySignature name="handleWheel" type="boolean" />

กำหนดว่าจะให้เลื่อนล้อเมาส์ตอน focus อยู่ที่ฟิลด์เพื่อเพิ่ม/ลดค่าได้หรือไม่ ค่าเริ่มต้น (default): false

<Callout type="info">
  **Note**

  ทำงานเฉพาะตอนฟิลด์ focus อยู่เท่านั้น ไม่ทำงานถ้า isDisabled หรือ isReadOnly
</Callout>

<HandleWheelDemo />

```tsx
<InputNumber handleWheel defaultValue={10} />
```

### isReadOnly

<PropertySignature name="isReadOnly" type="boolean" />

เมื่อเป็น true ผู้ใช้แก้ไขค่าไม่ได้ แต่ยังคง focus เลือกข้อความ และคัดลอกค่าได้ตามปกติ

ค่าเริ่มต้น (default): false

<ReadOnlyDemo />

```tsx
<InputNumber isReadOnly defaultValue={10} />
```

### isDisabled

<PropertySignature name="isDisabled" type="boolean" />

เมื่อเป็น true ปิดการใช้งานทั้งหมด พิมพ์และ focus ไม่ได้ แสดงผลจางลงเพื่อสื่อว่าใช้งานไม่ได้

ค่าเริ่มต้น (default): false

<DisabledDemo />

```tsx
<InputNumber isDisabled defaultValue={10} />
```

## Accessibility

InputNumber ทำหน้าที่เป็น spinbutton (มีปุ่มเพิ่ม/ลดค่าและปุ่มลูกศรบนคีย์บอร์ด) จึงตั้ง role และ aria-value* attribute ให้เองอัตโนมัติตาม ARIA spinbutton pattern มาตรฐาน โดยไม่ต้องตั้งค่าอะไรเพิ่ม:

- role เป็น spinbutton (แทน textbox ปกติ) — screen reader จะประกาศว่าเป็นปุ่มปรับค่า ไม่ใช่แค่ช่องพิมพ์ข้อความ
- aria-valuenow คือค่าตัวเลขดิบปัจจุบัน (ไม่มี comma หรือสัญลักษณ์ใด ๆ)
- aria-valuetext คือข้อความที่แสดงจริงในช่อง (รวม format ถ้ามี เช่น "$1,234.50") ให้ screen reader อ่านแบบเดียวกับที่ตาเห็น
- aria-valuemin / aria-valuemax มาจาก min / max โดยตรงถ้ามีการตั้งค่าไว้

<Callout type="info">
  **Note**

  aria-valuenow และ aria-valuetext อัปเดตตามสิ่งที่พิมพ์อยู่แบบสด ๆ ทันทีที่พิมพ์ (ไม่ต้องรอ blur/Enter) ตรงกับสิ่งที่แสดงบนหน้าจอเสมอ แม้จะยังไม่ commit เป็นค่าจริงก็ตาม — ต่างจาก value/onChange ที่ยังทำงานตาม commit model ตามปกติ (ดูหัวข้อ value) ถ้าสิ่งที่พิมพ์ยังไม่ใช่ตัวเลขที่สมบูรณ์ (เช่น พิมพ์แค่เครื่องหมายลบ) จะไม่ใส่ aria-valuenow ให้ชั่วคราวจนกว่าจะพิมพ์ต่อจนเป็นตัวเลขได้ เมื่อยังไม่มีค่า (value เป็น null และไม่ได้บังคับ isRequired) จะไม่ใส่ aria-valuenow/aria-valuetext เลย ตรงตามที่ ARIA spec แนะนำสำหรับ spinbutton ที่ยังไม่มีค่า
</Callout>

นอกจากนี้ prop hint (ข้อความคำอธิบายที่แสดงใต้ input) จะเชื่อมกับ input ผ่าน aria-describedby ให้อัตโนมัติเสมอ โดยสร้าง id ให้เองและ merge เข้ากับ aria-describedby ที่ส่งมาเองด้วย ถ้ามี
